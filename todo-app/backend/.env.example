# Server
PORT=4000

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=todo_app

# Allowed origin for CORS (frontend dev server)
CLIENT_ORIGIN=http://localhost:5173
